
## Changes

### 1. `src/routes/add-communication.tsx`
- Add a note directly under the page subtitle (muted info-box style matching existing disclaimer/preview cards):
  > "Content is finalized in Apollo and scheduled via Workdesk. All new or edited communications go through a single review step before going live, regardless of which team submits them."
- Keep the "Submit for Approval" button label unchanged.
- Change the success toast in `submit()` from `"[name] submitted for Category Manager approval"` to `"[name] submitted for approval"`.
- Add a small info box (subtle bordered/muted card with an `Info` lucide icon) directly above the Submit button:
  > "Check the Send Calendar to see if this can be clubbed with an existing send before submitting."

### 2. New route `src/routes/notifications.send-calendar.tsx`
- New `createFileRoute("/notifications/send-calendar")` page rendered inside `AppShell`.
- Gated to `role === "internal_ops"` (matches Add Communication pattern); otherwise `<Navigate to="/notifications" />`.
- Header: title "Send Calendar" with a `CalendarDays` icon, plus subtitle:
  > "Use this view to check if a new communication can be clubbed with something already going out, instead of sending it separately."
- Body: a simple date-grouped list (not a full calendar grid) of 5–6 mock scheduled comms spread across the next 14 days and across categories. Each entry shows:
  - Comm name
  - Category color tag (reuse `colorClasses[...].badge` from `mock-data.ts` for visual parity)
  - Target vendor segment when applicable (e.g. "All vendors", "Grocery – Tier 1", "Fashion vendors", "New vendors (< 90 days)"); omit the segment row when not applicable.
- Sample entries (inline mock array in the file, no persistence):
  1. Today + 1d — "Weekly payout summary" — `finance_payments` — All vendors
  2. Today + 2d — "PO acceptance reminder" — `reminders` — Grocery – Tier 1
  3. Today + 4d — "Monthly performance report" — `reports_analytics` — All vendors
  4. Today + 6d — "Password rotation notice" — `account_access` — (no segment)
  5. Today + 9d — "New catalog upload window" — `daily_ops` — Fashion vendors
  6. Today + 12d — "Q3 dispute resolution deadline" — `action_required` — New vendors (< 90 days)
- Styling matches existing cards in `notifications.index.tsx` / `add-communication.tsx` (rounded-xl border, muted section headers for date groups, small text, existing spacing).

### 3. `src/components/layout/AppShell.tsx`
- Add a new sidebar nav item "Send Calendar" (icon: `CalendarDays`) linking to `/notifications/send-calendar`, visible only when `role === "internal_ops"` (both sub-roles). Placed adjacent to the existing "Add Communication" item to keep Internal Ops actions grouped.

### 4. `src/routeTree.gen.ts`
- Auto-regenerated by the TanStack Router Vite plugin once the new route file exists; no manual edit intended, but the generated tree will include `/notifications/send-calendar` under the root route.

## Out of scope
- No changes to notification data model, role context, other routes, styling tokens, or backend logic. Purely presentational + a new mock-data screen.
